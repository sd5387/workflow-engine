# Minimal Workflow Engine (Assignment)

This repository implements a minimal workflow/agent engine with a FastAPI backend.
It demonstrates nodes, shared state, branching, looping, a tool registry and an example Code-Review mini-agent.

## Project structure
